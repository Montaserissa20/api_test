// Cypress support file (auto-loaded before specs)
// Add custom commands or global config here.

// Note: The Cucumber preprocessor works without importing its commands here.
// Some versions expose a `commands` subpath, others do not. To avoid
// version-specific resolution errors, we skip that import.
